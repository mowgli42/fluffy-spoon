# Local recipe creation server only (not used on Vercel static deploy)
ENABLE_RECIPE_CREATE=true
FLASK_SECRET_KEY=generate-a-long-random-string-here

# Optional: show "Create Recipe" on the deployed site (must be https in production)
# RECIPE_GENERATOR_URL=https://your-recipe-creator.example.com/
